# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Android / Gradle
/android/.gradle/
/android/captures/
/android/local.properties
*.keystore
*.jks
key.properties

# Generated Unity AR export (regenerated via Unity "Flutter > Export Android";
# ~5 GB of native libs/il2cpp output — never commit). Unity source lives in the
# separate Unity project, not in this repo.
/android/unityLibrary/
/assets/unity/

# .NET backend build output
backend/bin/
backend/obj/

# Local env / secrets (none committed; keep it that way)
.env
*.local

# Firebase / Cloudinary config — kept local. Use the *.example templates as a
# guide and run `flutterfire configure` to generate your own.
/android/app/google-services.json
/ios/Runner/GoogleService-Info.plist
!*.example

# OS / editor / tooling
Thumbs.db
.vscode/
.claude/
TODO.md
